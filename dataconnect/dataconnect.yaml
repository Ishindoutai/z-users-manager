specVersion: "v1"
serviceId: "z-users-manager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "z-users-manager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
